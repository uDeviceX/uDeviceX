= compute volume of a mesh
:lext: .adoc
:src: ../../../../src

Compute a an area for every triangle of the mesh.

[source,cpp]
----
include::{src}/mesh/tri_area/imp.h[tag=interface]
----
<1> in initialization triangles are stored
<2> compute areas

`nm` is a number of meshes.
