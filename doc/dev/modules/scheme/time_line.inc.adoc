= Time line

Keeps current simulation time.

[source,cpp]
----
include::{src}/scheme/time_line/imp.h[tag=interface]
----
<1> should be called every time step
<2> return `true` if `n*interval` is in between current and previus
    timesteps, in other words has the simulation just crossed
    `n*interval` for some `n`.
