== linar coordinate transformation

=== interface

[source,cpp]
----
include::{src}/math/tform/imp.h[tags=interface]
----
<I> allocate/deallocate

<C> create `Tform`

<U> using `Tform` to transform coordinates

<L> loging and dump of the `Tform` state
