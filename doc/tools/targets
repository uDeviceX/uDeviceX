#!/bin/sh

ADOC=`find . -type f \( -iname "*.adoc" ! -iname "*.inc.adoc" \)`

ext_html() (
    for f in $@; do
	echo "${f%.*}.html"
    done
)

PAGES=`ext_html $ADOC | xargs`

echo "PAGES = $PAGES" > make/target.mk
