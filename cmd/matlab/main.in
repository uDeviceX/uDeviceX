#!/bin/sh

. u.matlab.util

: ${MATLAB=%MATLAB%}

prog=u.matlab

usg () {
    msg "$prog [MATLAB OPTIONS]..."
    msg 'udx matlab wrapper'
    exit
}

if test $# -ne 0 && test "$1" = -h
then usg
fi

"$MATLAB" -nosplash "$@"
