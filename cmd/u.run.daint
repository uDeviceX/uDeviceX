#!/bin/bash

msg () { printf '%s\n' "$*" >&2; }
exec0() { # echo and exec
    msg cmd: "$@"; exec "$@"
}

n=$1; shift # n = x*y*z
x=$1; shift
y=$1; shift
z=$1; shift
e=$1; shift # executable

exec0 srun -n $n "$e" $x $y $z "$@"
