# TEST: conf.t0
mkdir -p tmp.conf
(
  cd tmp.conf
  u.conf `S=src u.cp.s` u/x <<!
  preved=1
!
cat Makefile
) > rm.out.txt

# TEST: conf.t1
mkdir -p tmp.conf
(
  cd tmp.conf
  u.conf `S=src u.cp.s` u/x <<!
  medved
!
cat Makefile
) > rm.out.txt
