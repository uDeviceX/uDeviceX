: ${AWK=awk}
: ${DBG=0}

e () {
  if test ! "$DBG" = 0
  then msg "cmd: $*"
  fi
  eval "$@"
}

msg () { echo >&2 "$*"; }
err () { msg "$prog: $@"; exit 2; }

assert_arg () {
    local m
    m="$1"; shift
    if test $# -eq 0; then err $m; fi
}
