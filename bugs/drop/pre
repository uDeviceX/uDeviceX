set -ue

safe_cp () {
    local f t
    f="$1"; shift
    t="$1"; shift
    if test ! -f "$f"; then printf 'pre: wrong file: %s\n' "$f"; exit 1; fi
    cp -- "$f" "$t"
}

safe_cp $s/data/sdf/yplates1/yplates.dat sdf.dat
