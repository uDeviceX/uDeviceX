### parse from opt file
# TEST: opt.t0
t=opt.cfg
u=u/parser
s=`pwd`
echo "a=42"   >  $t
echo "f=3.14" >> $t
:
u.conf "$s" "$u" "$s/conf/default.h" <<!
  run
!
{ u.make clean && u.make -j; } > /dev/null
./udx $t > parser.out.txt
