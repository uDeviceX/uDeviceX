# linal test
# TEST: math.linal
set -e -u
S=`pwd` U=u/math/linal C="$S/conf/default.h"
u.conf "$S" "$U" "$C" <<!
run
!
{ u.make clean && u.make -j; } > .make.log
u.run ./udx -- 2 1 5   3 4   -2      > linal.out.txt
