### Build out of source tree
# TEST: make.t1
t=/tmp/udx.$$
trap 'rm -rf $t' 0 1 2 3 15
:
s=`pwd`
mkdir $t
( cd $t
  u.conf "$s" "$s/conf/default.h" <<!
  run kBT=4.44302e-8 fsiforces=false
!
  { make clean && u.make -j; } > /dev/null
)
echo $?  > make.out.status
