# Compile all units
set -u
for U in `ls -d u/* u/*/* u/*/*/* u/*/*/* u/*/*/*/*`
do if test ! -f "$U/make/i"; then continue; fi
   echo >&2 "compile $U"
   u.conf0 "$U"
   { u.make clean && u.make -j; } > /dev/null
done