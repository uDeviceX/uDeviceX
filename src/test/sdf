# TEST: sdf.0
set -eu
S=`pwd` U=u/sdf C=conf/default.h
u.conf "$S" "$U" "$C" <<!
run
!
u.make clean && u.make -j
cp "$S"/data/sdf/cyl1/cyl.dat  sdf.dat
for x in `seq 0 3.0 10.0`
do
   u.run ./udx $x 0 0
done > sdf.out.txt
