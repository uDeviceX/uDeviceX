# TEST: case.0
set -eu
S=`pwd` U=u/x C="$S/conf/default.h"
c="$S/data/cells/rbc/498.off"
UDX=abcd S="$S" u.case -f $c rbc.off -b pwd -d case0 $U "$C" <<!
run
!
if test -e case0/abcd; then echo ok; else echo fail; fi > case.out.txt
