# velocity control
vcon = {
     active = true
     type   = "cart"
     U      = [1.0, 1.0, 0.0]
     log_freq    = 500
     adjust_freq = 500
     sample_freq = 1
}

# start from a 0 force
bforce = {
    type = "constant"
    f = [0., 0., 0.]
}
