### Build u/safe out of source
# TEST: safe.t1
t=/tmp/udx.$$
trap 'rm -rf $t' 0 1 2 3 15
:
u=u/safe
s=`pwd`
mkdir $t
( cd $t
  u.conf "$s" "$u" "$s/conf/default.h" <<!
  DBG_PEEK kBT=4.44302e-8 fsiforces=false
  run
!
  { make clean && u.make -j; } > /dev/null
)
echo $?  > make.out.status
