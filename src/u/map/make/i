f0 () { find "$1" -name '*.cu' -o -name '*.cpp' -o -name '*.h' -type f ; }
f () ( for d; do f0 "$d"; done )

f $U inc mpi d utils utils/map cloud hforces forces rnd frag
echo inc/conf.h msg.cpp msg.h 

# add dir
echo $U $U/lib
