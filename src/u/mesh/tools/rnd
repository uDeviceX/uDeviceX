#!/usr/bin/awk -f


function rnd(l, h) { return (h - l) * rand() + l }
function rnd3() {
    x = rnd(xl, xh); y = rnd(yl, yh); z = rnd(zl, zh)
}

BEGIN {
    N = ARGV[1]
    if (!length(N)) N = 100

    xl = yl = zl = -1.5
    xh = yh = zh =  1.5

    while (N--) {
	rnd3()
	print x, y, z
    }
}
