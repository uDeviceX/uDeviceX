#!/bin/bash

msg () { printf '%s\n' "$@" | cat >&2; }
usg () {
    msg  'adapts udx to systems'
    msg  './configure [source dir] [unit dir]'
    msg  '[source dir] : path to ./src'
    msg  '[unit dir]   : path to unit'
    exit
}

echo run | u.conf "$@" conf/default.h
