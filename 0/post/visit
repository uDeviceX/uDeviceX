set -u

rm0() { test -f "$1" && rm "$1"; } ### TODO ###
vi () { visit 2>.visit -nosplash -noconfig -sessionfile "$1"; }

rm0 r/00000.ply

vi.ln /tmp/l/r.visit r/*.ply -- /tmp/l/w.visit h5/wall.xmf
vi `pwd`/post/s.session
