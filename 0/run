set -ux
. ./util

safe_cp /scratch/snx1600/lisergey/pachinko/big/sdf.dat sdf.dat

small  () { x=7   y=2 z=1; }
normal () { x=14  y=4 z=1; }
big    () { x=28  y=8 z=1; }

s=../src
u=u/x
c=conf.def.h
normal

u.conf "$s" "$u" "$c" <<!
    run
!
{ make clean && u.make -j; } > /dev/null
u.strtdir . $x $y $z
u.batch $x $y $z ./udx 30:00
