set -ue

. ./util
. ./tsdf

s=../src
c=conf.def.h

# [01]
safe0

# domain types: oneA,    oneB, cyl, small, normal, big
cyl

# [01]
strt1

# force[AB]
# force=1e-1; forceA
v=0.8; shift=$1; forceB

sc=0.75 radius=3.0 rbc1 # rbc[1]
p_lina                  # p_[test|lina]

Domain="XS=$xs YS=$ys ZS=$zs"
u.conf "$s" "$u" "$c" <<!
    $Safe
    $Domain
    $Force
    $Rbc
    $Param
    $Bop
    $Restart
    run
!
{ make clean && u.make -j; } > .make.log

run