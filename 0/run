set -ue

. ./util
. ./tsdf

s=../src
u=u/x
c=conf.def.h

# domain types: oneA,    oneB, cyl, narrow small, normal, big
cyl

# [01]
strt0

# force[AB]
# force=1e-1; forceA
v=0.4; shift=10; forceB

sc=0.75 radius=3.0 rbc1 # rbc[1]
p_lina                  # p_[test|lina|stick]

Domain="XS=$xs YS=$ys ZS=$zs"
u.conf "$s" "$u" "$c" <<!
    $Domain
    $Force
    $Rbc
    $Param
    $Bop
    $Restart
    run
!
{ make clean && u.make -j; } > .make.log

run
