set -u

: ${DBG=0}

msg () { echo >&2 "$@"; }
e0 () {
   if test "$DBG" != 0; then msg "cmd: $@"; fi
   eval "$@"
}
e () {
   e0 "$@"
   if test $? -ne 0; then msg "fail: $@"; exit 2; fi
}

e pandoc "$@" --atx-headers -f docbook -t markdown "$i"
