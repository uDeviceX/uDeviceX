load("expr.mac") $
load("tone.mac") $
load("util.mac") $

L: 3/2*w(t300, R0);

draw2d(
  w(t300, explicit(''D, 'r, -R0, R0)), w(t300, explicit(-''D, 'r, -R0, R0)),
  'color='red,
  w(t217, explicit(''D, 'r, -R0, R0)), w(t217, explicit(-''D, 'r, -R0, R0)),
  'xrange=[-L, L], 'yrange=[-L, L]);
