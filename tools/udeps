#!/bin/bash

# u.deps and u.obj are from
# see git@gitlab.ethz.ch:mavt-cse/clarity

# run from src
l=`find . -name '*.cu' -o -name '*.h'`
o=make/dep.mk
u.deps $l > $o

echo upd: $o >&2
echo upd: $o >&2
