= download

see link:bootstrap[bootstrap]

= install toolls

....
make
....

= after adding files

run from src/

....
../tools/deps
....

= update gh-pages
....
git push origin `git subtree split --prefix doc master`:gh-pages --force
....
